/** @type {import('tailwindcss').Config} */
export const content = ["./src/**/*.{js,jsx,ts,tsx}"];
export const theme = {
  extend: {},
};
export const plugins = [];

// Add the darkMode option to enable class-based dark mode
export const darkMode = 'class';
